# ArcScan API Key (Get from https://testnet.arcscan.app)
VITE_ARCSCAN_API_KEY=your-api-key-here

# ARC Name Registry Contract Address
# Deploy the contract first, then add the address here
# See DEPLOYMENT.md for instructions
VITE_REGISTRY_ADDRESS=0x0000000000000000000000000000000000000000

# Deployment Private Key (KEEP SECRET - NEVER COMMIT TO GIT)
# Only needed for contract deployment
# Remove this line after deployment or keep in .env (gitignored)
DEPLOYER_PRIVATE_KEY=your-private-key-here
